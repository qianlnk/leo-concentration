# 专注力训练系统 TODO 列表

## Goals
- 引入“AI奇奇博士”引导孩子按计划训练，过程给予正向反馈。
- 接入语音/视频模块，识别孩子的回答与行为并量化评分。
- 完善任务记录与统计，形成可追踪的训练档案与报告。

## Product Todo
- 奇奇博士引导
  - 设计博士形象、语气与对话脚本（欢迎语、提示语、鼓励语）。
  - 首页引导流程：读取计划书→生成今日训练路线→逐项启动任务。
  - 训练中语音气泡与动态贴纸（完成/出错即时鼓励）。
- 训练路线与难度
  - 将计划书结构化为“任务清单 + 等级”（如数字迷踪3×3→4×4→5×5）。
  - 每个任务定义“步骤/关卡/时长建议/升级条件/评分规则”。
  - 支持按孩子表现动态调整难度与任务顺序。
- 语音模块（数字顺风耳）
  - 前端接入麦克风：`getUserMedia({audio:true})`，录制与回放。
  - 题干播报与随机数字序列生成，支持多轮与等级（长度、速度）。
  - 语音识别方案评估：
    - MVP：上传音频→后端离线识别（如集成 `Vosk` 或 `whisper.cpp`），比对期望序列（如 7、6、4）。
    - 识别失败或暂不可用时，保留“教师标注”入口。
  - 评分与反馈：正确率、响应延迟、节奏稳定性；生成鼓励语和贴纸。
- 视频模块（指令相反国）
  - 前端接入摄像头：`getUserMedia({video:true})`，录制与回放。
  - 题干引导（“举右手就举左手”等），规范化指令集与时间窗。
  - 行为识别路线：
    - MVP：录制视频并保存，提供“教师审阅标注”打分。
    - 增强：接入姿态识别（前端 MediaPipe/WASM 或服务端 `gocv`/OpenCV），检测左右手、方向、动作时序；对“反着做”进行规则判定。
  - 评分与反馈：合规率、反应时、连续正确次数；即时鼓励语与动画。
- 即时鼓励与激励机制
  - 完成与进步触发音效、粒子动画、贴纸收集。
  - 分数段反馈模板（如≥80%提示“超能小卫士”）。
  - 每日达成小目标与周累计勋章。
- 报告与可视化
  - 每日成果：总时长、任务完成数、得分分布、错误类型。
  - 曲线图：耗时-天、正确率-天、难度进阶记录。
  - 会话详情页：各任务分组用时、每轮正确/错误、音频转写、视频评分。

## Data & Backend Todo
- 任务/会话模型扩展
  - `children(id,name,age,profile)`。
  - `plans(id,child_id,date,title)`、`plan_tasks(plan_id,training_id,level,order)`。
  - 扩展 `sessions`：`child_id, plan_id, task_level, task_slug, started_at, ended_at, duration_seconds, score`。
  - 新表 `events(session_id, ts, type, payload_json)`（记录每次点击/识别结果/反馈触发）。
  - 新表 `media(session_id, kind, path, duration, transcript, result_json, score)`（音频/视频文件与识别结果）。
- 记录完善（当前不完善项补齐）
  - 每关用时独立记录（如数字迷踪各等级、平衡超人每轮、全身唤醒每组）。
  - 正确/错误细分为“类型 + 值”（如错序、漏答、动作不符）。
  - 将备注换为结构化事件与结果（JSON），减少纯文本。
  - 评分规则固化（正确率、反应时、稳定性），会话聚合出最终分数与评语。
- 存储与性能
  - 媒体文件保存目录 `data/media/<date>/<session>`，定期归档。
  - 大文件上传分片/断点（后续）。
  - 隐私与授权：录制前弹窗征询同意，可选择仅音频/仅视频。

## Frontend Todo
- 首页重构为“博士引导中心”
  - 左侧博士角色气泡引导，右侧任务卡片（今日路线）。
  - 开始→进行中→完成的状态与进度条。
- 任务页面统一框架
  - 题干区、计时区、操作区、反馈区、媒体状态区。
  - 组件化：计时器、等级选择、鼓励弹窗、贴纸面板。
- 媒体采集 UI
  - 清晰的录制按钮与状态（录制中/已保存/重试）。
  - 音频波形、视频缩略图预览。
  - 失败重试与权限引导。

## Integration & Tech Choices
- 语音识别：优先评估 `Vosk`（离线，Go 可集成），备选 `whisper.cpp`（需本地编译与模型）。
- 姿态识别：前端 MediaPipe（WASM）或后端 `gocv`，按设备能力选择；MVP 先人工标注。
- 安全与隐私：本地存储、删除与导出，家长与老师权限。

## Milestones
- M1：博士引导首页 + 任务编排 + 完善会话/事件/媒体表结构。
- M2：语音采集与识别 MVP（数字顺风耳）+ 评分与鼓励。
- M3：视频采集与人工标注 MVP（指令相反国）+ 报告呈现。
- M4：数字迷踪/平衡超人/全身唤醒全面接入事件与分组用时。
- M5：前端动画与贴纸体系 + 每日/周勋章。
- M6：姿态识别与自动判定（迭代优化）。
- M7：隐私与家长控制、导出与备份。

